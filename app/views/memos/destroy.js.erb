<%# modalを閉じる処理 %>
$("#memoModal").modal("hide");
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();

<%# 画面の書き換え %>
$('[data-model_name="memo"]').filter('[data-id="<%= @memo.id %>"]').remove();
