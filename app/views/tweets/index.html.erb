<p><id"notice"><%= notice %></p>
<h5>投稿一覧</h5>
    <% if user_signed_in? %>
        <%= link_to '新規投稿', new_tweet_path %>
    <% end %>
    <div class="scroll">
        <%= render @tweets %>
    </div>




